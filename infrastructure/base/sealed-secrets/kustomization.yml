apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sealed-secrets

resources:
  - sealed-secrets-namespace.yml
  - sealed-secrets-rbac.yml
  - sealed-secrets-deployment.yml
  - sealed-secrets-service.yml

labels:
  - includeSelectors: true
    pairs:
      app: sealed-secrets
      managed-by: argocd
