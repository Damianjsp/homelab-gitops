apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: n8n
  namespace: ai-services
  labels:
    app: n8n
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: ai-services-n8n-stripprefix@kubernetescrd
spec:
  rules:
  - host: dahousesp.asuscomm.com
    http:
      paths:
      - path: /n8n
        pathType: Prefix
        backend:
          service:
            name: n8n
            port:
              number: 5678
