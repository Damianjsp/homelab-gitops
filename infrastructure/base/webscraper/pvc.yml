apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: webscraper-data
  namespace: webscraper
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
