apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: redis-n8n-secrets
  namespace: ai-services
  labels:
    app: redis-n8n
spec:
  encryptedData:
    # TODO: Replace with sealed secret values
    # Generate sealed secret with:
    # echo -n "redis-secure-password" | kubectl create secret generic redis-n8n-secrets --dry-run=client --from-file=redis-password=/dev/stdin -o yaml | kubeseal -f -
    redis-password: ""
  template:
    metadata:
      name: redis-n8n-secrets
      namespace: ai-services
      labels:
        app: redis-n8n
    type: Opaque
