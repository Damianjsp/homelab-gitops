apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-n8n-data-pvc
  namespace: ai-services
  labels:
    app: redis-n8n
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  resources:
    requests:
      storage: 5Gi
