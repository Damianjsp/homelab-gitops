apiVersion: v1
kind: Service
metadata:
  name: litellm-proxy
  namespace: litellm
  labels:
    app: litellm-proxy
spec:
  type: LoadBalancer
  selector:
    app: litellm-proxy
  ports:
  - name: http
    port: 4000
    targetPort: 4000
    protocol: TCP
