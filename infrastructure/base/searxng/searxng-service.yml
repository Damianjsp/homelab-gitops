apiVersion: v1
kind: Service
metadata:
  name: searxng
  namespace: searxng
  labels:
    app: searxng
    component: search-engine
spec:
  type: NodePort
  selector:
    app: searxng
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30888
      protocol: TCP
